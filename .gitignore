__pycache__/
*.py[cod]
*.egg-info/
.venv/
.uv/
build/
dist/
.pytest_cache/
.ruff_cache/
.coverage
.env
.env.local
.DS_Store

# A conventional place to keep a corpus inside the checkout. docker-compose.yml has no
# default and requires DATASHARE_DATA_DIR to be set explicitly; this rule exists so that
# pointing it here cannot commit corpus documents.
/data/
